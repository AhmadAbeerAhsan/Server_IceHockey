services:
  ok-api:
    image: ok-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081/tcp"
      - "8080:8080/udp"
    develop:
      watch:
        - action: rebuild
          path: .
